<div class="text-center">
  <h2>{{username}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{blog-content}}</p>
  <p>Created by {{username.name}} on {{format_date date_created}}</p>
  <p>
    {{!-- <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed --}}
  </p>
  <p>
    <button class="btn btn-primary"Add Tech Info!></button>
  </p>
</div>
